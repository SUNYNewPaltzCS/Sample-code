const chai = require('chai');

describe('receiver.js', function() {
    it('separate location data from form data within a JSON object', function() {
        chai.assert(true,true,"NOT TRUE");
    });
})