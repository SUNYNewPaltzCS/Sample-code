describe('fusiontable.js', function() {
    it('should return a list of fusion tables the user has access to');
    it('should return all of the column headers for a given fusion table');
    it('should submit a query to the fusion table and return the results');
})